# Task Completion

- For code changes, first use Serena symbol retrieval as required by `AGENTS.md`.
- Keep edits minimal and do not overwrite unrelated user changes.
- After changes, run available targeted checks/tests. If no tests are configured, report that clearly.
- When dependency changes are needed, update `backend/pyproject.toml` consistently with the project's package manager.