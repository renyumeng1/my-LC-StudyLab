# Environment Management

This project uses `uv` to manage the Python/backend environment. Prefer commands such as `uv add --project backend ...`, `uv run --project backend python ...`, and `uv run --project backend pytest ...` instead of direct `pip`/`python`/`pytest` invocations.
